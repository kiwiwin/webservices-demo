allprojects {
    apply plugin: 'idea'
}

subprojects {
    apply plugin: 'java'

    dependencies {
        repositories {
            mavenCentral()
        }

        testCompile "junit:junit:4.11"
    }
}