allprojects {
    apply plugin: 'idea'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'war'
    apply plugin: 'jetty'

    dependencies {
        repositories {
            mavenCentral()
        }

        testCompile "junit:junit:4.11"
    }
}